import{afterEach,describe,expect,it,vi}from'vitest';import{listVideoModels}from'./client';afterEach(()=>vi.unstubAllGlobals());describe('model catalog',()=>{it('loads video models with session credentials',async()=>{const f=vi.fn().mockResolvedValue(new Response(JSON.stringify({ok:true,items:[]})));vi.stubGlobal('fetch',f);await listVideoModels();expect(f).toHaveBeenCalledWith('/api/models?type=video',expect.objectContaining({credentials:'include'}))})})
